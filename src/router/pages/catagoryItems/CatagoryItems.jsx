import { useLoaderData } from "react-router-dom";


const CatagoryItems = () => {
    const data = useLoaderData()
    console.log(data);
    return (
        <div>
       
        </div>
    );
};

export default CatagoryItems;